describe("The palette", function () {
    "use strict";
    it("should be able to calculate the correct colour given an appropriate histogram", function () {
        var histogram = jim.histogram.create(),
            palette = jim.palette.create();
    });
});